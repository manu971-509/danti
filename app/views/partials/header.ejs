<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>DANTICAM - Boutique en ligne de caméras de surveillance</title>
    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />

    <!-- Bootstrap Icons -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css"
      rel="stylesheet"
    />

    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <header>
      <div class="header">
        <img
          src="https://cdn-icons-png.flaticon.com/128/9720/9720711.png"
          alt="DANTICAM Logo"
        />
        <h2 class="ms-1">DANTICAM</h2>
      </div>

      <nav>
        <div class="nav d-flex justify-content-between align-items-center p-3">
          <span class="d-flex gap-3">
            <a href="/" class="text-decoration-none">Accueil</a>
            <a href="/product" class="text-decoration-none">Nos produits</a>
            <a href="/product" class="text-decoration-none">Nouveautés</a>
          </span>

          <span class="d-flex gap-3">
            <% if (isAuthenticated) { %> <% if (user.roles === 'admin') { %>
            <li class="nav-item">
              <a class="nav-link active text-white" href="/admin"
                >Administration</a
              >
            </li>
            <% } %>
            <li class="nav-item">
              <a class="nav-link active text-white" href="/logout"
                >Déconnexion</a
              >
            </li>
            <li class="nav-item">
              <span class="nav-link text-white"
                ><i class="bi bi-person"></i> <%= user.firstname%></span
              >
            </li>
            <% } else { %>
            <li class="nav-item">
              <a class="nav-link active text-white" href="/login">Connexion</a>
            </li>
            <% } %>
            <a href="/panier.ejs" class="text-decoration-none">
              <i class="bi bi-cart"></i>
              <span class="badge bg-info">0</span>
            </a>
          </span>
        </div>
      </nav>
    </header>
  </body>
</html>
